# GPU-ready requirements for VISTA (Python 3.12+)
# Uses CUDA 12.1 builds for PyTorch. TensorFlow uses the unified wheel (GPU if CUDA present).

# Use the official PyTorch CUDA 12.1 wheel index
--extra-index-url https://download.pytorch.org/whl/cu121

# Core web
Flask==3.1.2
Werkzeug==3.1.3

# Vision and arrays
opencv-python==4.9.0.80
numpy==1.26.4

# YOLO (uses PyTorch GPU if installed)
ultralytics==8.3.204

# PyTorch CUDA 12.1 wheels (install with index URL above)
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121

# TensorFlow/Keras (GPU-enabled if CUDA present)
tensorflow==2.16.2
keras==3.11.3

# Optional: LLM support (comment out if not needed)
transformers==4.57.0
accelerate==1.10.1

# Notes:
# - Install via: pip install -r new_requirements.txt
# - If PyTorch index URL causes resolution issues, install torch/torchvision/torchaudio first with the same index, then pip install -r new_requirements.txt
# - OpenCV here is CPU build; for OpenCV CUDA you need a custom wheel or to build from source.
